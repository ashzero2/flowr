package com.ash.flowr.ui.theme

import androidx.compose.ui.graphics.Color

// Still Ink — Forest Ledger palette
// Primary: deep forest green (not blue) — calm, grounded, distinctive for a finance app

// Brand: forest green spectrum
val InkDeep = Color(0xFF1C3D2F)        // primary: deep forest
val InkMint = Color(0xFFC0EDD8)        // primary container: light mint
val InkForest = Color(0xFF7ABDA0)      // primary for dark mode

// Paper surfaces — warm, not cold blue-white
val Paper = Color(0xFFF9F8F5)          // page background: unbleached cotton
val PaperCard = Color(0xFFFFFFFF)      // card surface: white lifted off warm background
val PaperSubtle = Color(0xFFF1EFE9)    // surface variant: faintly aged page

// Ink tones — warm grays, not cold slate-blue
val InkBlack = Color(0xFF1A1814)       // primary text: warm near-black
val InkGray = Color(0xFF78746C)        // secondary text: warm medium gray
val InkFaint = Color(0xFFC4C0B8)       // borders
val InkLine = Color(0xFFE0DDD6)        // subtle dividers

// Dark mode surfaces
val NightSurface = Color(0xFF131210)
val NightCard = Color(0xFF1E1C19)
val NightSubtle = Color(0xFF2E2C28)
val NightText = Color(0xFFE8E4DC)
val NightMuted = Color(0xFFB0ACA4)

// Semantic
val IncomeGreen = Color(0xFF2E7B55)    // income: medium forest green
val MutedText = InkGray               // alias used across components
val TransferMuted = InkGray           // transfers are net-zero, shown neutral

// Category colors — grounded, not neon
val CategoryFood = Color(0xFFB85C2A)          // clay / terracotta
val CategoryTransport = Color(0xFF2E5F8A)     // prussian blue
val CategoryShopping = Color(0xFF6B4A9E)      // deep violet
val CategoryEntertainment = Color(0xFFA8395A)  // wine / burgundy
val CategoryUtilities = Color(0xFF2A6B62)     // teal-slate
val CategoryHealth = Color(0xFF8B2323)        // deep crimson
val CategoryOther = Color(0xFF5C5850)         // warm charcoal
